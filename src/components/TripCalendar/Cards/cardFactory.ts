import React from "react";
import { CoreEvent, EventTypes } from "../../../data/event";

/*export function getCardForEvent(event: CoreEvent): React.FC {
    switch (event.type) {
        case EventTypes.TripStart:
        case EventTypes.TripEnd:
            return <div />
    }
}*/